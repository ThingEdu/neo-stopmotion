# docs/01-specs/features/frame-review-delete/manifest.yml
feature_key: "frame-review-delete"
title: "Xem lai va xoa frame khi chup stop-motion"
owner_spec: "ba"

scope: "app"   # app | firmware | both
# Ghi chú scope: hành động xoá frame bất kỳ cần chọn qua UI (chuột/phím),
# không thể thực hiện đủ bằng nút phần cứng ThingBot.
# Xoá frame cuối nhanh (UNDO) đã có lệnh UART UNDO — giữ nguyên.
# Không thêm lệnh UART mới cho tính năng này.

status: "draft"        # draft | approved | in_progress | done
ui: "yes"              # yes | no
design_required: "yes" # yes | no
# design_required=yes: UX Designer cần spec màn hình review (thumbnail strip,
# highlight frame được chọn, nút xác nhận xoá) trước khi dev code UI.
design_ref: "docs/01-specs/features/frame-review-delete/design-spec.md"
# Ghi chú: design-spec.md đang được UX Designer soạn song song.

staged_config:
  enabled: false
  due_date: ""
  ticket: ""

blocked: true
blocked_reasons:
  - "Q1: PO cần quyết xoá frame bất kỳ hay chỉ nâng cấp UNDO (xem Cau hoi lam ro trong spec.md)"
  - "Q2: Giới hạn xoá tối đa bao nhiêu lần / có Undo-of-delete không?"
  - "Q3: Thumbnail image provider: QQuickImageProvider hay file:// URL?"
